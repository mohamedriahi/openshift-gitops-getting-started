apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: spring-petclinic
  name: spring-petclinic
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: spring-petclinic
    weight: 100
  wildcardPolicy: None
  host: router-customer.apps-ocp01-xxnym.shiva.cloud-temple.com
